# Attention Score 分析備忘錄

## 📌 概述

Attention Score（注意力分數）是深度學習模型用來識別**哪些輸入特徵最重要**的機制。本文件說明 Attention Score 的原理、應用，以及在三高疾病預測研究中的運用。

---

## 🎯 什麼是 Attention Score？

### 簡單比喻

想像醫生看病歷：
- 病歷有 100 頁資料
- 醫生會**特別注意**某些關鍵資訊（如血壓趨勢、家族史）
- **Attention Score** 就是模型告訴你：「我在這 100 個特徵中，最關注哪幾個」

### 技術定義

**Attention Score** = 模型對每個輸入特徵的重要性評估

- **數值範圍**：0-1 之間（經過 softmax 標準化）
- **數值越高**：表示模型認為該特徵對預測結果越重要
- **總和為 1**：所有特徵的 Attention Score 加起來 = 1

### 數學公式

```
Attention(Q, K, V) = softmax(Q × K^T / √d_k) × V
```

**參數說明**：
- **Q (Query)**：查詢向量，代表「我要找什麼」
- **K (Key)**：鍵向量，代表「每個特徵是什麼」
- **V (Value)**：值向量，代表「特徵的實際內容」
- **d_k**：鍵向量的維度（用於縮放，防止梯度消失）

**計算步驟**：
1. 計算相似度：`Q × K^T`（哪些特徵與查詢相關）
2. 縮放：`/ √d_k`（防止數值過大）
3. 標準化：`softmax(...)`（轉換成 0-1 的分數）
4. 加權求和：`× V`（根據重要性加權特徵）

---

## 🔬 Taiwan MTL (2025) 的 Attention 分析

### 研究背景

- **論文**：Multitask learning multimodal network for chronic disease prediction
- **作者**：Hsinhan Tsai et al., National Taiwan University
- **期刊**：Scientific Reports (2025)
- **DOI**：10.1038/s41598-025-99554-z

### 模型架構中的 Attention

#### Multi-Head Self-Attention (MHSA) 層

```
輸入：180 個 ICD 代碼 embedding
    ↓
MHSA 計算疾病之間的相關性
    ↓
輸出：加權後的疾病特徵表示 + Attention Scores
```

**超參數設定**：
- Number of heads: 1
- Key dimension: 16
- Value dimension: 8

**作用**：
- 學習哪些疾病之間有關聯
- 例如：高血脂 ↔ 心臟病（Attention Score 高）
- 例如：白內障 ↔ 心臟病（Attention Score 低）

---

### Attention Score 分析流程

#### Step 1: 選擇高風險患者

```
所有患者 (555,124 人)
    ↓ 篩選條件
選取預測分數最高的前 2000 名患者
    ↓
這些是模型「最有把握」會發病的人
```

**為什麼選前 2000 名？**
- 高置信度預測，模型最確定的案例
- 分析這群人的共同疾病模式
- 找出哪些疾病組合最容易導致慢性病

#### Step 2: 計算 ICD 配對的 Attention Score

**每個患者的醫療紀錄**：
```
患者 A：過去 10 年，180 個 ICD 代碼

模型計算每兩個 ICD 之間的 Attention Score：
┌─────────────────────────────────────┐
│ ICD₁ (高血脂) ←→ ICD₂ (糖尿病)      │
│   Attention Score = 0.85            │ ⭐ 高相關
├─────────────────────────────────────┤
│ ICD₁ (高血脂) ←→ ICD₃ (白內障)      │
│   Attention Score = 0.12            │ 低相關
├─────────────────────────────────────┤
│ ICD₂ (糖尿病) ←→ ICD₄ (腎衰竭)      │
│   Attention Score = 0.91            │ ⭐ 超高相關
└─────────────────────────────────────┘
```

#### Step 3: 統計高 Attention Score 的 ICD

在前 2000 個高風險患者中：
- 統計哪些 ICD 配對的 Attention Score 最常出現
- 識別模型認為最重要的疾病關聯

---

### 發現的三類關鍵疾病

#### ✅ 類別 1：可修改風險因子

| ICD 代碼 | 疾病名稱 | Attention Score 特點 | 醫學意義 |
|---------|---------|---------------------|---------|
| 272.0 | 純高膽固醇血症 | 經常與慢性病高相關 | 高血脂是心血管疾病、糖尿病的重要風險因子 |
| 272.2 | 混合型高血脂 | 在高風險患者中頻繁出現 | 文獻證實：可透過飲食、藥物控制 |
| 272.4 | 其他高血脂 | 與多種慢性病關聯 | **這是可改變的！** 干預重點 |

**關鍵發現**：
- Attention 機制自動識別出**高血脂是慢性病的重要前兆**
- 與醫學文獻完全一致 ✅
- 證明模型學到了真實的醫學知識

---

#### 🏥 類別 2：多重慢性病（Multimorbidity）

| ICD 代碼 | 疾病名稱 | 為什麼 Attention Score 高？ |
|---------|---------|---------------------------|
| 274.9 | 痛風 | 與代謝症候群相關，老年人常見 |
| 366.10 | 老年性白內障 | 老化相關疾病，高齡患者常見 |
| 571.40 | 慢性肝炎 | 慢性病患者容易併發多種疾病 |
| 585 | 慢性腎衰竭 | 糖尿病、高血壓的常見併發症 |
| 715.36 | 下肢骨關節炎 | 老年慢性病，與活動量、代謝相關 |
| 715.90 | 骨關節炎（未明示） | 同上 |
| 721.3 | 腰薦椎脊椎病 | 老年退化性疾病 |

**醫學意義**：
- 患有**一種慢性病**的人，容易發展**多種慢性病**
- 文獻證實：Multimorbidity（多重慢性病）在老年人中普遍
- Attention 機制捕捉到了這個重要的共病模式 ✅

**實務啟示**：
- 如果患者已有一種慢性病 → 需要更密切監控其他慢性病風險
- 預防策略應該考慮多重疾病管理

---

#### 🆕 類別 3：新興風險因子

| ICD 代碼 | 疾病名稱 | 新發現！ | 近期研究證據 |
|---------|---------|---------|-------------|
| 300.00 | 焦慮症 | 精神健康 → 慢性病風險 ↑ | 壓力影響免疫系統、內分泌 |
| 300.4 | 神經性憂鬱症 | 憂鬱 → 生活習慣惡化 → 疾病發生 | 憂鬱患者運動少、飲食差 |

**醫學意義**：
- 這是**較新的研究方向**（過去較少被關注）
- 近期研究才開始探討：精神健康 ↔ 慢性病的雙向關係
- Attention 機制發現了文獻中才剛開始探討的關聯 ✨

**論文評論**：
> "這些發現使焦慮和憂鬱成為值得進一步調查的潛在風險因子"

**實務啟示**：
- 慢性病預防應該納入**心理健康篩檢**
- 壓力管理可能是預防慢性病的重要策略

---

## 📊 Attention Score 可視化範例

### 範例 1：單一患者的 Attention Heatmap

假設**患者 X**被預測為高風險，分析過去 10 年的醫療紀錄：

```
患者 X 的醫療紀錄 Attention Score：
┌──────────────────────────────────────────┐
│ 2014年：高血脂 (272.2)   ████████  0.85  │ ⭐ 最高
│ 2015年：痛風 (274.9)     ██████    0.62  │
│ 2016年：高血脂 (272.0)   ███████   0.78  │
│ 2017年：焦慮症 (300.0)   ████      0.45  │
│ 2018年：關節炎 (715.3)   ███       0.38  │
│ 2019年：白內障 (366.1)   ██        0.28  │
│ 2020年：慢性肝炎 (571.4) ██        0.25  │
└──────────────────────────────────────────┘
              Attention Score (0-1)
```

**模型的思考過程**：
> "這個患者有兩次高血脂診斷（272.2, 272.0），Attention Score 很高（0.85, 0.78）
>
> → 我認為**高血脂是最重要的風險信號**
>
> → 加上痛風（代謝問題），焦慮症（壓力）
>
> → 綜合判斷：未來 5 年發展糖尿病/心臟病的機率很高"

---

### 範例 2：多患者的 ICD 關聯熱力圖

在 2000 個高風險患者中，統計 ICD 配對的平均 Attention Score：

```
        糖尿病  心臟病  高血壓  高血脂  腎衰竭
糖尿病   1.00   0.78   0.82   0.85   0.71
心臟病   0.78   1.00   0.88   0.79   0.65
高血壓   0.82   0.88   1.00   0.76   0.69
高血脂   0.85   0.79   0.76   1.00   0.58
腎衰竭   0.71   0.65   0.69   0.58   1.00

🔴 深紅 (0.8-1.0)：強相關
🟠 橙色 (0.6-0.8)：中等相關
⚪ 白色 (0.0-0.6)：弱相關
```

**關鍵發現**：
- **糖尿病 ↔ 高血脂**：Attention Score = 0.85（最強）
- **高血壓 ↔ 心臟病**：Attention Score = 0.88（超強）
- **糖尿病 ↔ 腎衰竭**：Attention Score = 0.71（常見併發症）

---

## 🆚 Attention Score vs SHAP Value

### 核心差異

| 比較項目 | Attention Score | SHAP Value |
|---------|----------------|------------|
| **計算時機** | 模型訓練時學習 | 模型訓練後計算 |
| **理論基礎** | 神經網路可學習權重 | 賽局理論 (Shapley Value) |
| **適用模型** | 僅神經網路（需 Attention 層） | 所有模型（model-agnostic） |
| **輸出含義** | 相對重要性 (0-1) | 特徵貢獻值（可正可負） |
| **計算成本** | 低（推論時直接得到） | 高（需重複計算） |
| **理論保證** | 無 | 有（滿足公平性等公理） |
| **視覺化** | 熱力圖、條形圖 | 瀑布圖、蜂群圖、力圖 |
| **可比較性** | 無法跨模型比較 | 可跨模型比較（LR, RF, NN） |

---

### 何時使用？

#### ✅ 使用 Attention Score

適用情境：
- 想要展示神經網路的「內建可解釋性」
- 需要**即時解釋**（推論時直接輸出，無需額外計算）
- 分析**不同任務**關注的特徵差異（MTL 場景）
- 研究**特徵之間的關聯**（如 ICD 配對）

優點：
```python
# 推論時直接得到，速度快
predictions, attention_scores = model(patient_data)
print(attention_scores)  # 立即知道哪些特徵重要
```

缺點：
- 只能用在有 Attention 機制的神經網路
- 無法與其他模型（LR、RF）比較

---

#### ✅ 使用 SHAP Value

適用情境：
- 需要**公平比較不同模型**（LR, RF, SVM, NN）
- 要求理論保證和數學嚴謹性
- 分析**單一樣本**的詳細貢獻
- 解釋為什麼模型做出某個預測

優點：
```python
# 可以比較所有模型
shap_lr = explain_model(logistic_regression, X_test)
shap_rf = explain_model(random_forest, X_test)
shap_nn = explain_model(neural_network, X_test)

# 公平比較：LR vs RF vs NN 的特徵重要性
```

缺點：
- 計算成本高（需要重複推論很多次）
- 深度神經網路計算特別慢

---

### 建議：兩者都用 🎯

**研究策略**：
1. **SHAP 用於主要模型比較**
   - 回答：Interpretable vs Black-box 哪個好？
   - 公平比較所有模型的可解釋性

2. **Attention 用於展示神經網路的額外優勢**
   - 證明：神經網路不是完全黑盒
   - 展示：內建可解釋性 + 即時解釋能力

---

## 🎓 在三高預測研究中的應用

### Taiwan MTL vs 我們的研究

| 項目 | Taiwan MTL (2025) | 我們的研究（規劃） |
|------|-------------------|-------------------|
| **預測目標** | 糖尿病、心臟病、中風、高血壓（4 種） | 高血糖、高血壓、高血脂（3 種三高） |
| **輸入資料** | ICD 診斷代碼序列（10年，180個） | HRS 調查資料（問卷 + 生物標記） |
| **特徵類型** | 醫療紀錄 + 個人資訊 | 人口統計 + 生活方式 + 生理指標 |
| **時序結構** | 有（每 2 個月的 ICD 序列） | 有（Y-2 → Y-1 → Y 時間點） |
| **資料來源** | 台灣健保資料庫 HWDC | Health and Retirement Study |
| **樣本數** | 555,124 | 待確認（約 1,155 有 3+ 次記錄） |
| **MTL 架構** | Hard Parameter Sharing | 可參考 |
| **Attention** | Multi-Head Self-Attention | 可借鑑概念 |

---

### 關鍵差異：不能直接使用 ICD Embedding

**Taiwan MTL 的方法**：
```
ICD 代碼序列：[250.0, 401.9, 272.2, ...]
    ↓
Word2Vec Embedding（將相似疾病分組）
    ↓
180 個疾病 embedding 向量
    ↓
Multi-Head Self-Attention（學習疾病關聯）
```

**我們的資料**：
```
HRS 問卷資料：
- 血壓：135 mmHg
- 血糖：110 mg/dL
- BMI：27
- 年齡：65 歲
...（沒有 ICD 代碼！）
```

**為什麼不能直接用？**
- ❌ 他們：有 ICD 診斷代碼序列 → 可用 Word2Vec 做疾病 embedding
- ❌ 我們：沒有 ICD 代碼，只有數值型問卷變數 → **不適用 ICD embedding**

---

### 可以借鑑的部分 ✅

雖然不能直接用 ICD embedding，但可以借鑑以下概念：

#### 1. Multi-Task Learning 架構

```python
# 同時預測三高（而非建立三個獨立模型）
class ThreeHighMTL(nn.Module):
    def __init__(self):
        self.shared_layers = nn.Sequential(...)  # 共享層
        self.hypertension_head = nn.Linear(...)   # 高血壓
        self.hyperglycemia_head = nn.Linear(...)  # 高血糖
        self.dyslipidemia_head = nn.Linear(...)   # 高血脂
```

**好處**：
- 捕捉三高之間的相關性
- 參數效率高（1/3 參數量）

---

#### 2. Feature-level Attention

雖然沒有 ICD 序列，但可以對**數值型特徵**使用 Attention：

```python
class FeatureAttention(nn.Module):
    """特徵層級的 Attention"""
    def __init__(self, num_features):
        super().__init__()
        self.attention_weights = nn.Linear(num_features, num_features)

    def forward(self, x):
        # x: [batch_size, num_features]
        scores = self.attention_weights(x)
        attention = torch.softmax(scores, dim=1)
        weighted_features = x * attention
        return weighted_features, attention
```

**應用範例**：
```
輸入特徵（28 個）：
- Y-2 特徵（10 個）：SBP, DBP, BMI, Glucose, ...
- Y-1 特徵（10 個）：SBP, DBP, BMI, Glucose, ...
- Δ 特徵（8 個）：ΔSBP, ΔDBP, ΔBMI, ...
    ↓
Feature Attention（學習哪些特徵最重要）
    ↓
Attention Score：
  - ΔSBP: 0.85 ⭐ 最重要
  - Y-1_SBP: 0.72
  - 年齡: 0.68
  - ...
```

---

#### 3. Task-specific Attention（多任務 Attention）

每個任務（高血壓、高血糖、高血脂）可能關注不同的特徵：

```python
class TaskSpecificAttention(nn.Module):
    """每個任務有自己的 Attention 權重"""
    def __init__(self, num_features, num_tasks=3):
        super().__init__()
        # 每個任務一個 Attention 層
        self.task_attentions = nn.ModuleList([
            nn.Linear(num_features, 1) for _ in range(num_tasks)
        ])

    def forward(self, x, task_id):
        # 根據任務選擇對應的 Attention
        scores = self.task_attentions[task_id](x)
        attention = torch.softmax(scores, dim=1)
        return x * attention, attention
```

**預期發現**：
```
高血壓任務的 Attention：
  - SBP, DBP → 高分
  - Glucose → 低分

高血糖任務的 Attention：
  - Glucose, HbA1c → 高分
  - SBP → 中等分（相關但不是主因）

高血脂任務的 Attention：
  - Cholesterol, TG, LDL → 高分
  - BMI → 中等分
```

---

#### 4. Temporal Attention（時序 Attention）

我們有 Y-2 → Y-1 → Y 的時序資料，可以用 Attention 學習哪個時間點最重要：

```python
class TemporalAttention(nn.Module):
    """時間點的 Attention"""
    def __init__(self, feature_dim):
        super().__init__()
        self.temporal_attention = nn.Linear(feature_dim, 1)

    def forward(self, t1_features, t2_features):
        # Stack: [batch_size, 2, feature_dim]
        temporal_features = torch.stack([t1_features, t2_features], dim=1)

        # 計算每個時間點的重要性
        scores = self.temporal_attention(temporal_features)
        attention = torch.softmax(scores, dim=1)

        # 加權求和
        weighted = (temporal_features * attention).sum(dim=1)
        return weighted, attention
```

**應用**：
```
患者 A：
- Y-2 Attention: 0.35
- Y-1 Attention: 0.65 ⭐
→ 模型認為「最近的檢查」更重要

患者 B：
- Y-2 Attention: 0.72 ⭐
- Y-1 Attention: 0.28
→ 模型認為「早期異常」是警訊（即使 Y-1 改善）
```

---

## 💡 實作建議

### 建議的模型架構（完整）

```python
class ThreeHighPredictionWithAttention(nn.Module):
    def __init__(self, num_features=28, num_tasks=3):
        super().__init__()

        # 1. Feature-level Attention
        self.feature_attention = FeatureAttention(num_features)

        # 2. Shared layers (MTL)
        self.shared_layers = nn.Sequential(
            nn.Linear(num_features, 128),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(128, 64),
            nn.ReLU()
        )

        # 3. Task-specific heads with Attention
        self.hypertension_attention = nn.Linear(64, 1)
        self.hyperglycemia_attention = nn.Linear(64, 1)
        self.dyslipidemia_attention = nn.Linear(64, 1)

        self.hypertension_head = nn.Linear(64, 1)
        self.hyperglycemia_head = nn.Linear(64, 1)
        self.dyslipidemia_head = nn.Linear(64, 1)

    def forward(self, x):
        # Feature Attention
        x_attended, feature_scores = self.feature_attention(x)

        # Shared representation
        shared_repr = self.shared_layers(x_attended)

        # Task-specific predictions with Attention
        hyp_attn = torch.sigmoid(self.hypertension_attention(shared_repr))
        glu_attn = torch.sigmoid(self.hyperglycemia_attention(shared_repr))
        lip_attn = torch.sigmoid(self.dyslipidemia_attention(shared_repr))

        hypertension_pred = torch.sigmoid(self.hypertension_head(shared_repr * hyp_attn))
        hyperglycemia_pred = torch.sigmoid(self.hyperglycemia_head(shared_repr * glu_attn))
        dyslipidemia_pred = torch.sigmoid(self.dyslipidemia_head(shared_repr * lip_attn))

        return {
            'predictions': {
                'hypertension': hypertension_pred,
                'hyperglycemia': hyperglycemia_pred,
                'dyslipidemia': dyslipidemia_pred
            },
            'attention_scores': {
                'feature_attention': feature_scores,
                'task_attention': {
                    'hypertension': hyp_attn,
                    'hyperglycemia': glu_attn,
                    'dyslipidemia': lip_attn
                }
            }
        }
```

---

### 分析 Attention Score 的步驟

#### Step 1: 訓練模型

```python
model = ThreeHighPredictionWithAttention(num_features=28)
# 訓練過程...
```

#### Step 2: 提取高風險患者的 Attention

```python
# 在測試集上推論
results = model(X_test)

# 選取高風險患者（預測分數 > 0.8）
high_risk_mask = results['predictions']['hypertension'] > 0.8
high_risk_patients = X_test[high_risk_mask]
high_risk_attention = results['attention_scores']['feature_attention'][high_risk_mask]

# 平均 Attention Score
mean_attention = high_risk_attention.mean(dim=0)
```

#### Step 3: 視覺化 Feature Importance

```python
import matplotlib.pyplot as plt

feature_names = ['SBP_Y-2', 'DBP_Y-2', 'BMI_Y-2', ..., 'ΔSBP', 'ΔDBP', ...]
attention_scores = mean_attention.cpu().numpy()

plt.figure(figsize=(10, 6))
plt.barh(feature_names, attention_scores)
plt.xlabel('Average Attention Score')
plt.title('Feature Importance for High-Risk Patients')
plt.tight_layout()
plt.show()
```

#### Step 4: 與 SHAP 比較

```python
import shap

# SHAP 解釋
explainer = shap.Explainer(model, X_train)
shap_values = explainer(X_test)

# 比較
comparison_df = pd.DataFrame({
    'Feature': feature_names,
    'Attention_Score': attention_scores,
    'SHAP_Importance': abs(shap_values.values).mean(axis=0)
})

print(comparison_df.sort_values('Attention_Score', ascending=False))
```

---

## 📈 預期研究成果

### 論文可以展示的內容

#### 1. 證明神經網路的可解釋性

**對比**：
```
傳統觀點：
  神經網路 = 黑盒，無法解釋

我們的研究：
  神經網路 + Attention = 可解釋
  - Attention Score 顯示特徵重要性
  - 與 SHAP 分析一致
  - 與醫學知識一致
```

#### 2. 發現疾病特定的風險因子

**預期發現**：
```
高血壓模型的 Attention：
  - SBP 變化（ΔSBP）: 0.92 ⭐
  - Y-1 收縮壓: 0.78
  - 年齡: 0.65
  - BMI: 0.42

高血糖模型的 Attention：
  - 血糖變化（ΔGlucose）: 0.88 ⭐
  - Y-1 空腹血糖: 0.82
  - BMI: 0.58
  - 年齡: 0.61

→ 不同疾病關注不同的特徵！
```

#### 3. 多任務學習的共享知識

**MTL 的優勢**：
```
單任務學習 (STL)：
  - 高血壓模型只學高血壓
  - 高血糖模型只學高血糖
  - 高血脂模型只學高血脂

多任務學習 (MTL)：
  - 共享層學習「代謝健康」的通用表示
  - 三個任務互相幫助
  - Attention 顯示共同風險因子（如 BMI、年齡）
```

---

## 🎯 總結與建議

### 核心要點

1. **Attention Score = 模型的「注意力」分布**
   - 告訴你模型認為哪些特徵最重要
   - 數值範圍 0-1，總和為 1

2. **Taiwan MTL 的成功經驗**
   - 用 Attention 發現高血脂、多重慢性病、精神健康等風險因子
   - 結果與醫學文獻一致，驗證模型可信度

3. **Attention vs SHAP**
   - Attention：神經網路內建，即時解釋
   - SHAP：跨模型比較，理論保證
   - **建議兩者都用**

### 對你的研究的建議

#### ✅ 主要策略：SHAP + Attention 雙軌分析

**SHAP 分析**（主要）：
- 比較所有模型（LR, DT, RF, SVM, NN）
- 回答：Interpretable vs Black-box 哪個好？
- 公平比較可解釋性

**Attention 分析**（加分項）：
- 展示神經網路的額外優勢
- 即時解釋能力
- 發現疾病特定的風險模式

#### ✅ 建議的模型設計

1. **Feature-level Attention** - 哪些特徵最重要
2. **Task-specific Attention** - 不同疾病關注不同特徵
3. **Temporal Attention** - Y-2 vs Y-1 哪個時間點更重要

#### ✅ 預期貢獻

1. **方法論貢獻**：
   - 證明 MTL + Attention 可應用於三高預測
   - 不需要 ICD 代碼，問卷資料也能用 Attention

2. **實務貢獻**：
   - 識別可修改風險因子（如 BMI、血壓變化）
   - 提供即時可解釋的預測結果

3. **理論貢獻**：
   - 證明神經網路不是完全黑盒
   - Attention 與 SHAP 的比較分析

---

## 📚 延伸閱讀

### 相關文獻

1. **Taiwan MTL (2025)**
   - DOI: 10.1038/s41598-025-99554-z
   - 檔案：[Taiwan_MTL_2025_中文翻譯.md](../references/Taiwan_MTL_2025_中文翻譯.md)

2. **Attention 機制應用指南**
   - 檔案：[attention_mechanism_guide.md](../references/attention_mechanism_guide.md)

3. **SHAP 相關**
   - 檔案：[AUC_memo.md](../literature_notes/AUC_memo.md)

### 相關概念

- [Multi-Task Learning](Model_Comparison_Plan.md)
- [Feature Selection](Feature_Selection_Marginal_Utility.md)
- [Model Interpretability](Class_Imbalance_in_Medical_Prediction.md)

---

**建立日期**: 2025年
**最後更新**: 2025年
**作者**: 紀伯喬
**用途**: 碩士論文研究備忘錄
